# services/jellyfin/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jellyfin
  namespace: services
  annotations:
    kubernetes.io/ingress.class: cilium
spec:
  rules:
    - host: jellyfin.agate.home
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jellyfin
                port:
                  number: 8096
