apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: omada-controller
spec:
  template:
    spec:
      securityContext:
        runAsUser: 2000
        runAsGroup: 2000
        fsGroup: 2000
        fsGroupChangePolicy: OnRootMismatch
