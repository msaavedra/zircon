apiVersion: v1
kind: Service
metadata:
  name: mosquitto-service
spec:
  selector:
    app: mosquitto
  type: NodePort
  ports:
    - protocol: TCP
      port: 1883
      targetPort: 1883
      nodePort: 31883
