image:
  repository: eclipse-mosquitto
  tag: 2.0.15
  pullPolicy: IfNotPresent

service:
  type: NodePort
  ports:
    mqtt:
      port: 1883
      nodePort: 31883

mosquitto:
  persistence:
    enabled: true
    existingClaim: mosquitto-data
    size: 2Gi

  config: |
    allow_anonymous true
