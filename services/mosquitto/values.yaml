image:
  repository: eclipse-mosquitto
  tag: 2.0.15
  pullPolicy: IfNotPresent

mosquitto:
  persistence:
    enabled: true
    existingClaim: mosquitto-data
    size: 2Gi

  config: |
    allow_anonymous true
